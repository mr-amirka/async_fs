language: rust
rust:
  - nightly
os: linux
dist: xenial

cache: cargo

sudo: required

before_install:
  - sudo bash ./kcov-install.sh

script:
  - cargo test
  - sudo bash ./coverage.sh

notifications:
  email:
    - mr.amirka@ya.ru
  slack:
    on_success: always
    on_failure: always
